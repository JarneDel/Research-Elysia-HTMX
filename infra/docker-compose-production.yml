version: '3'
services:
  api:
    image: jarnedel.registry.jetbrains.space/p/main/containers/api:0.0.3
    build:
      context: ../
      dockerfile: Dockerfile
    ports:
      - '8080:80'
    env_file:
      - ../.env.production.local

